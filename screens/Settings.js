import React, { Component } from "react";
import { Text } from "react-native";

export default class SettingsScreen extends Component {
  static navigationOption = { title: "Settings" };
  render() {
    return <Text>Settings</Text>;
  }
}
